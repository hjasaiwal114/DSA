""" question -Given a Integer array A[] of n elements. Your task is to complete the function PalinArray. Which will return 1 if all the elements of the Array are palindrome otherwise it will return 0.
    
 """






# Your task is to complete this function
# Function should return True/False or 1/0
def PalinArray(arr ,n):
    # Code here
    
    for i in range(n):
        for i in range(n):
            if str(arr[i])!=str(arr[i])[::-1]:
                return 0
            
    return 1

        
        
    



#{ 
 # Driver Code Starts
# Driver Program
if __name__=='__main__':
    t=int(input())
    for i in range(t):
        n = int(input())
        arr = list(map(int, input().strip().split()))
        if PalinArray(arr, n):
            print(1)
        else:
            print(0)
# Contributed By: Harshit Sidhwa

# } Driver Code Ends



#............... imsme return m 1 likh dena hota h is nhi chla to retun 1 hi a jayga filhal ::-1 se dekh ise agge dekhte h inverse bhi dekh

